<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 其中命名空间指向与该文件同名的DAO层接口 -->
<mapper namespace="com.ims.dao.INewsDAO">
	<!-- 通过resultMap解决实体类属性与数据库字段名不一致的问题 -->
	<resultMap type="com.ims.entity.News" id="newsResult">
		<!-- 其中id为主键，result为普通字段 -->
		<!-- property对应实体类属性，column对应数据库字段名（查询结果集中的字段名） -->
		<id property="id" column="id"/>
		<result property="cid" column="cid" />
		<result property="title" column="title" />
		<result property="type" column="type" />
		<result property="author" column="author" />
		<result property="abst" column="abst" />
		<result property="content" column="content" />
		<result property="thumbnail" column="thumbnail" />
		<result property="createtime" column="createtime" />
		<result property="updatetime" column="updatetime" />
		<result property="display" column="display" />
		<result property="rd" column="rd" />
	</resultMap>
	<!-- 增删改查 -->
	<insert id="add"  parameterType="java.lang.String">
		${_parameter}
	</insert>
	<delete id="del"  parameterType="java.lang.String">
		${_parameter}
	</delete>
	<update id="alter"  parameterType="java.lang.String">
		${_parameter}
	</update>
	<select id="query" resultMap="newsResult" parameterType="java.lang.String" >
		${_parameter}
	</select>

	 <!-- 插入数据后获得自增主键的值 -->
	<insert id="insert" keyProperty="id" keyColumn="id">
		INSERT INTO news(cid,title,type,author,abst,content,thumbnail,updatetime,display,rd) 
		VALUES(#{cid},#{title},#{type},#{author},#{abst},#{content},#{thumbnail},#{updatetime},#{display},#{rd})
	</insert>
	<!-- 修改用户内容 -->
	<update id="update">
		UPDATE news set cid=#{cid}, title=#{title}, type=#{type}, author=#{author}, abst=#{abst}, 
		content=#{content}, thumbnail=#{thumbnail}, updatetime=#{updatetime}, 
		display=#{display}, rd=#{rd} WHERE id=#{id}
	</update>
	<!-- 根据id删除用户 -->
    <delete id="delete">
        DELETE FROM news WHERE id=#{id}
    </delete>
	<!-- 根据id查询用户 -->
	<select id="findById" resultMap="newsResult">
		SELECT * FROM news WHERE id=#{id}
	</select>
    <!-- 查询所有用户 -->
	<select id="findAll" resultMap="newsResult">
		SELECT * FROM news
    </select>
    
    <select id="total" resultType="int">
		SELECT count(*) from news where type=#{type}
	</select>

</mapper>